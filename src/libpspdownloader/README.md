# Usage

* Simplest setup
```c
#include <downloader.h>
int main(int args, char *argv[]) {
  download_file(URL, OUTPUT_FILE_LOCATION);
}
```

### Debug vs Release lib
* Debug: Much more on screen verbose output
* Release: Quiet version no output to screen

